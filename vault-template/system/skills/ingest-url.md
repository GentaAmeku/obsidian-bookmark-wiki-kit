# ingest-url

Single URL is converted into a Source Note.

## Document ingest

PDF / Excel / Word / PowerPoint URLs are converted with MarkItDown when available. Extracted Markdown is saved under `assets/extracted/`, and summarized Source Notes are saved under `sources/documents/`.
